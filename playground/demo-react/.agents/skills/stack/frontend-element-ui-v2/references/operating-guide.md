<!-- V3 generated bundle support: do not treat this file as a summary -->
# frontend-element-ui-v2 完整执行指南

## 能力边界

前端技术栈专项：Element UI 2.x 项目的表单、表格、弹窗、分页、上传、主题和反馈工程能力，遵守 Vue 2 与项目二次封装边界。

本文件是入口 Skill 的可执行补充，不替代入口规则。入口负责触发和路由；本指南负责把输入、操作、证据、质量门禁和失败回退落到可执行步骤。详细资料、模板和评测必须保留在本 Bundle 内，不能因为上下文预算删除。

## 输入契约

1. 任务原文、用户明确的范围和不可改变的边界。
2. 当前项目入口规则、Harness 设置、技术栈事实和既有变更文档。
3. 与本能力直接相关的源码、配置、资源、测试、脚本和构建描述。
4. 当前工作区状态、用户已有修改、允许使用的命令和恢复点。

开始前将事实分为“已观察、合理推断、待确认、冲突”四类。目录名、旧摘要、模型记忆和相似关键词不能单独作为事实。

## 执行协议

1. 写出目标、非目标、工作模式、当前阶段、退出条件和风险等级。
2. 建立证据索引：每条结论记录路径或命令、观察时间、版本/哈希和敏感信息脱敏方式。
3. 从最小影响范围开始读取；涉及跨模块、删除、迁移、权限、数据或发布时扩大影响账本。
4. 按入口 Skill 的专项检查顺序执行。每一步都记录输入、决定、实际操作、产物和未执行项。
5. 先执行最窄的静态/契约验证，再按风险扩大到构建、测试、浏览器、数据或发布验证。
6. 输出中文回执：已读取、已选择的能力、实际修改、证据、未知项、风险、回退点和下一步。

## 质量门禁

- 正向任务必须能说明为什么加载本能力，负向任务必须能说明为什么排除。
- 每个产物能追溯到需求、影响项或验证证据；没有来源的推断只能放在待确认区。
- 未执行的命令标为“未观察”，超时、工具缺失和环境阻断不能写成通过。
- 不读写秘密，不把用户文件和 Harness 生成文件混为一谈，不覆盖用户修改。
- 失败必须保留原始错误、已完成步骤和一条可继续执行的回退路径。

## 交付物

- 结构化任务/报告或本次变更产物。
- 变更文件清单和影响账本更新。
- 验证命令、工作目录、退出码、输出路径和未覆盖风险。
- 可恢复检查点或备份引用；需要人工审批的项不得自批。

## 失败回退

| 情况 | 回退动作 |
| --- | --- |
| 目标或范围不清 | 回到需求确认，列出最少待确认问题，停止越界修改 |
| 发现新影响或冲突 | 暂停当前批次，更新影响账本和计划，重新评审范围 |
| 工具/环境不可用 | 保存原始输出，改用项目已有等价命令或标记未观察 |
| 契约、安全、数据一致性失败 | 阻断交付，恢复到最近检查点，不用摘要掩盖失败 |
| 用户文件已修改 | 保留用户版本，生成差异和人工合并候选 |

## 完成判定

本能力只有在入口规则、相关支持资料、模板、正反例、验收清单和评测用例都可定位，并且本次任务的实际证据已经写出时才算使用完成。只有读取了入口文件不算完成。
