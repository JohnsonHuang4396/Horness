# 编码报告：{需求名称}

## 文档信息

- **需求名称**: [填写需求名称]
- **编码日期**: [YYYY-MM-DD]
- **关联任务清单**: [tasks.md 路径]

---

## 变更文件清单

### 新增文件

| 文件路径 | 分层 | 说明 |
| -------- | ---- | ---- |

### 修改文件

| 文件路径 | 变更内容 |
| -------- | -------- |

---

## 废弃文件声明

> **强制填写**：编码过程中创建但最终未采用的文件（中间草稿、路径变更前的旧文件等），必须在此声明并确认已从磁盘删除。若无废弃文件，填写"无"。**遗漏此项将导致 P6 质量门禁不通过。**

| 废弃文件路径 | 废弃原因 | 是否已删除 |
| ------------ | -------- | ---------- |
| 无           | -        | -          |

---

## 暂不实现清单

> 以下功能/文件在需求或设计阶段已标记为"暂不实现"，评审时将自动豁免，本次编码未实现：

| 功能/模块    | 对应文件       | 原因                           |
| ------------ | -------------- | ------------------------------ |
| [功能点名称] | [预留文件路径] | 后续迭代 / 待确认 / 依赖未就绪 |
| 无暂不实现项 | -              | -                              |

---

## 关键实现说明

{针对复杂逻辑的说明，不超过 200 字}

---

## 自检结果

- [ ] TypeScript 编译通过
- [ ] 无 `any` 类型使用
- [ ] 无跨层调用（pages 未直接调用 api）
- [ ] 单 .vue 文件不超过 600 行
- [ ] 配置项已抽离到 `useConfig.ts`
- [ ] 组件已声明 `defineOptions({name: 'XxxName'})`
- [ ] 自动导入已检查，无重复引入
- [ ] 废弃文件声明已填写（编码过程中产生的中间草稿已全部删除或标注）

---
